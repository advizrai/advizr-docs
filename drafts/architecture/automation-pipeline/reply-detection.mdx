# Reply Detection

The first stage of the lead generation pipeline. Reply detection monitors inbound email for prospect responses, classifies them by intent (interested, objection, unsubscribe, out-of-office), and routes them to the appropriate next step. This page covers the detection logic, classification criteria, and integration with the Gmail API.
