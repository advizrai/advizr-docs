# KB Supabase

The knowledge base Supabase instance powers the Agency KB system. It stores document chunks with pgvector embeddings, agent memory entries, directive metadata, and quality scoring records. This page covers the KB schema, the embedding pipeline, vector search configuration, and how the 9 MCP tools interact with the database.
