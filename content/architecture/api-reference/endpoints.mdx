# API Endpoints

Complete endpoint reference for all Advizr services. Lists every available route with its HTTP method, path, required parameters, request body schema, and response format. Organised by service - Agency KB API, text embedder, orchestrator, and client platform.
