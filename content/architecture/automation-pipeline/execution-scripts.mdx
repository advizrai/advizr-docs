# Execution Scripts

Reference for the 51+ deterministic Python scripts that power the automation pipeline. Each script maps to a specific directive and handles one discrete task - API calls, data transformations, file operations, or service integrations. No probabilistic AI runs inside execution scripts; they receive structured inputs and produce predictable outputs.
