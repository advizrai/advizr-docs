# Client Supabase

Each client organisation gets a dedicated Supabase project that stores all client-specific data - documents, objectives, chat history, proposals, and learning progress. This page covers the standard client schema, table relationships, RLS policies, and how the client database is provisioned during onboarding.
