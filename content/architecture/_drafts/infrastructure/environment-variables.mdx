# Environment Variables

Reference for every environment variable used across Advizr services. Covers API keys, database connection strings, service URLs, and third-party credentials. Each variable lists where it is required, where to obtain it, and which deployment platform holds it.
