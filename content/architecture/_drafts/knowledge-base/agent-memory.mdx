# Agent Memory

The agent memory system allows AI agents to persist discoveries, workarounds, and learned constraints across sessions and machines. Memories are saved via memory_save, retrieved via memory_search, and browsed chronologically via memory_timeline. This page explains the memory schema, retrieval ranking, and best practices for what to save.
