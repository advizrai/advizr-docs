# Runs and Workflows

Your Advizr team builds automated workflows that handle tasks for your business - things like generating reports, processing documents, analysing data, and executing multi-step processes. Each time a workflow executes, it creates a "run" that you can monitor and review. The Runs section shows you what is running, what has completed, and what the results are.

<Screenshot
  src="/images/platform/runs.png"
  alt="Runs and workflows"
  caption="Monitor automated workflow progress and review outputs."
/>

## What are workflows?

Workflows are automated sequences that your FDE builds to address your business objectives. For example:

- A workflow that generates a monthly financial summary from your uploaded data
- A workflow that processes incoming leads and enriches them with public data
- A workflow that analyses customer feedback and produces a trends report
- A workflow that drafts email sequences based on your brand guidelines

Each workflow is configured specifically for your business using the documents and context you have provided.

## How runs work

A run is a single execution of a workflow. Runs can be triggered in three ways:

- **By your FDE** - Your team member starts a run manually as part of their work
- **By you via Chat** - Ask in Chat to run a specific workflow and it can be triggered directly
- **On a schedule** - Some workflows run automatically at set intervals (daily, weekly, monthly)

<Callout type="info">
  Workflows are built by your Advizr team based on your objectives and proposals. You do not need to configure them yourself.
</Callout>

## Monitoring a run

<Steps>
  <Step title="Open the Runs section">
    Click Runs in the sidebar or navigate to it from a notification. You will see a list of recent runs with their status.
  </Step>
  <Step title="Check the status">
    Each run shows one of four statuses: **Triggered** (queued to start), **Running** (in progress), **Completed** (finished successfully), or **Failed** (something went wrong).
  </Step>
  <Step title="View the details">
    Click a run to see its step-by-step log. Each step shows what happened, how long it took, and whether it succeeded. For completed runs, the output is available to download or review.
  </Step>
</Steps>

## Reviewing outputs

When a run completes, its output appears in the run detail view. Depending on the workflow, outputs might include generated documents, data tables, analysis summaries, or status reports. You can download outputs or reference them in Chat.

## When a run fails

If a run fails, the detail view shows which step failed and why. Your FDE is automatically notified of failed runs and will investigate. You do not need to take action, but you can mention it in Chat if you want an update.

## Next steps

<LinkCard
  title="Analytics"
  description="Track workflow performance and measure the impact on your business."
  href="/docs/platform/features/analytics"
  icon="📊"
/>
